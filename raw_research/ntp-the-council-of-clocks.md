# ntp — the council of clocks

*A pointer to ntp.org, wired the way the vault wires pointers: as
doctrine, not as a dependency note. The constellation's entire ledger —
every dogfeed row, stream cursor, laps record, and multiD telemetry — is
timestamped in UTC, which means every breath we row has already been
tuned to the council of clocks. NTP is the fleet's time-keeping theorem,
running underneath everything, unnoticed the way a good invariant should
be.*

## the stratum, mapped

| the ntp layer | the constellation's shape |
|---|---|
| stratum 0 — the true clocks (atomic, GPS, the earth itself) | the invariant the fleet cannot bend: UTC, the manual's CLOCK audits (NOT resettable since the clock) |
| stratum 1 — the reference servers directly synced to stratum 0 | the first elders: the trusted anchors every lesser clock asks |
| stratum N — everything else, disciplined by consensus | every daemon: the sidecar, the dogfeed, the multidog — each syncs against the same mean, the way the pupil takes the geometric mean of the elders' softmaxes |
| leap seconds — the honest correction | the ledger's own discipline: when time's accounting drifts, you correct it openly and row the correction — never fake the width of the second |

## the project's own words (kept verbatim)

> NTP is what ensures the reliability of billions of devices around the
> world, under the sea, and even in space ... The NTP Project produces an
> open source Reference Implementation of the NTP standard ... develops
> the protocol and algorithmic standard that is used to communicate time
> between systems. — ntp.org, the Network Time Foundation

An open protocol, an open implementation, a consensus timebase: the
first global distributed system to run the whole planet on one honest
mean — and it did it decades before the word "open source" had the
meaning this vault gives it. Show me the mechanism, in every second of
every day, since before 1985.

## the honest note

The fleet has been running NTP's doctrine without ever naming it: a
single, honest, externally witnessed timekeeping mean that no single
clock may override. The riva breath is the minute; NTP is the hour; and
the Clock audits on the machine in the operator's room are the same
record in steel — non-resettable earnings since the clock, which is
another way of saying: time is the first ledger.

*ntp.org · the council of clocks · stratum N, disciplined by consensus ·
UTC as the one ledger · leap seconds rowed honestly · the constellation ·
fine touch from within · vaked.dev · 8b-is, 2026-09-18*