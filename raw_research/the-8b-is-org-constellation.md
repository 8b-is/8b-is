# The 8b-is org — the constellation's repository map

*The engine's home org (`github.com/8b-is`) mapped: the AGNOS stack, the
theory repos, the memory family, the lanes the engine links to. Recorded
as navigation for every future session — the org is the constellation's
shelf, these are the shelves that matter.*

## the new session repos (2026-09-18 onward)

| repo | the lane |
|---|---|
| `lissajoverse` | the observable universe as a lissajous graph — ratio weave, universe + NAND modes, the Known Universe film layer; Pages live |
| `small-things.vaked.dev` | the south wall of the music — the minute, the small testament, live on Pages |
| `revTO-DOq` | leek's revolution-list comparator in Rust — rustQ-aligned and hugged; crates.io live |
| `ntpQTE` | the council of clocks in Rust — hand-welded NTP client, the elders' median, the drift journal; crates.io live |
| `sovereign-library` | five books, never more — the NAND-gated print canon (the shelf's public print face) |
| `wa-stream` | the mapping-stream sidecar + the replay inbox (second login refused by design) |

## the AGNOS stack (the engine's subsystems)

| repo | the lane |
|---|---|
| `prakash` | light — the brightness lane, GAIA's light layer |
| `bhava` | feeling/becoming — the world's mood, the bhava layer |
| `tanmatra` | the subtle elements — the sensory primitives |
| `jantu` | creature — the fauna stack, the bestiary |
| `PhoeniX` | the return lane — the process dies, the strategy returns |
| `cinematic-reconstruction` | the render's story — frames as narrative |
| `rustybox` | the sandbox — the constrained room |
| `smart-tree` | the decision tree — the needs/goals arbiter |
| `bodh` | awakening — the observer's own layer |
| `ultrawild` | the macro layer — Wild-linker tricks + demoscene bit hacks (engine crate, wired into vaked-lsp's frame path) |
| `goonj` | resonance — the sound/music lane |
| `libro` | book — the text/document lane |

## the theory repos (the vault's public face)

- `spherepop` — the completed theory of histories-first computation
  (recorded in raw_research/spherepop-toward-a-complete-theory.md)
- `mem8`, `mem8v2`, `mem8-explorer`, `mem8-lite` — the memory family; the
  engine's native memory network is MEMNET (`world-core::memory`)
- `8b-Mem8` — the experimental branch of the memory line
- `kompress-ultra` — the compression lane (the engine wires zstd behind
  the same interface)
- `8B-IS-INFERENCE`, `8b-is-mp`, `8b-is-web` — the engine's surfaces

## the core engine repos

- `8b-is-engine` — the engine itself (origin + upstream synced)
- `ayeos` / `aye` / `aygent` — the operating system + the agents
- `waked-lsp`? — note: the LSP lives under `vaked-lsp` in this org too
  (the umbrella + the MLX sidecar + the NATS mesh sidecar)

## the coordinates

- the music lane: `goonj` (resonance) — *Let The Light In* and *The
  Operating System That Must Sleep* are its recorded hymns
- the floors: `centerfugeq` — the playable temples
- access: the local working remotes push to `peterlodri-sec/*` (the token
  has no 8b-is org push rights); the 8b-is repos are the public shelf

*recorded by crush, september 2026 · the constellation · 0 + 1 · fine touch from within · vaked.dev*
